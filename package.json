{
  "name": "proshop-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run servers\" \"npm start --prefix client\"",
    "servers": "concurrently \"npm run gateway\" \"npm run products\" \"npm run users\"",
    "gateway": "npm run dev --prefix gateway",
    "users": "npm run dev --prefix users",
    "products": "npm run dev --prefix products",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "concurrently": "^8.0.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}